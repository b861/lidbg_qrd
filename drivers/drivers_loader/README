修正：重新定义此模块会根据lidbg_qrd/soc/msm8x26/conf下的两个lidbg_drivers_list.conf和third_party_list.conf配置文件来执行，每个文件可以相当于一个shell脚本. 2014.5.15

修正：重新定义此模块的作用旨在创建一个需要lidbg去配合但又不需要lidbg去配合的环境，让其他组的其他需求能在shell中就完成的集体在这边实现，而不需要修改lidbg的代码结构. 2014.5.14
{
注意事项:
1:每一行的字符长度需大于等于3，否者做丢弃处理(被丢弃的指令会有类似的打印: err.third_party_analyze: bad cmd)
2:每一行都会转化为shell指令去执行(另新支持了cp指令和msleep指令)
3:msleep指令的单位是ms
}

此模块的建立主要是为了让其他组的模块加载与lidbg分离.2014.5.12
{
配置文件third_party.conf主要描述了需要加载的模块的绝对路径和驱动的设备节点的绝对路径,一条指令单独占一行，并且模块路径和设备节点路径以逗号作为分割.

注意事项:
1:每一行必须有逗号(分隔符)
2:设备节点以/开头，如果不需要修改设备节点，则值为null
3:支持注释,以#号开头
}

